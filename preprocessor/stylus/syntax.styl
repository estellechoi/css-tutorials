// out: syntax.css, compress: true

header {
    color: tomato;
    /* Parent reference */
    /* .container header */
    .container & {
        h1 {
            text-align: center;
        }
    }
}

/* Mixins */
/*
border-radius(val) {
    -webkit-border-radius: val
    -moz-border-radius: val
    border-radius: val
}

button {
    border-radius(10px);
}
*/

/* Transparent mixins */
border-radius() {
    -webkit-border-radius: arguments
    -moz-border-radius: arguments
    border-radius: arguments
}

.test header h1 {
    border: 1px solid dodgerblue;
    border-radius: 10px;
}

/* Variables */
button {
    width: w = 200px
    height: (w / 2)
}

/* Block property access */
.test section {
    background-color: dodgerblue;
    margin: 0 auto;
    width: 1000px;
    /* @width : 해당 중괄호 내에서의 width 값 */
    height: (@width / 2)
}

/* Robust feature-rich language */
/* regarding position.... */
-pos(type, args) {
    i = 0
    position: unquote(type)
    {args[i]}: args[i + 1] is a 'unit' ? args[i += 1] : 0
    {args[i += 1]}: args[i + 1] is a 'unit' ? args[i += 1] : 0
}

absolute() {
    -pos('absolute', arguments)
}

fixed() {
    -pos('fixed', arguments)
}

.position {
    background-color: yellow;
    width: 200px;
    height: (@width / 2)
    absolute: top 150px left 5px
}

/* Iteration ? */
table {
  for row in 1 2 3 4 5
    tr:nth-child({row})
      height: 10px * row
}

